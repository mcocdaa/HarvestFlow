name: default
type: collector
version: 1.0.0
description: Default file collector - scans folder for JSON files
author: HarvestFlow

# 密钥定义（本插件无需密钥）
secrets: []

# 插件配置
config:
  backend_entry: backend.py
