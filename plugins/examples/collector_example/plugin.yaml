name: collector_example
type: collector
version: 1.0.0
description: Example collector plugin - demonstrates how to build a custom collector
author: Your Name

# 密钥定义
secrets:
  - name: API_KEY
    description: API key for external service
    required: false

# 插件配置
config:
  backend_entry: backend.py
  custom_config:
    scan_interval: 60
    max_file_size: 10485760
