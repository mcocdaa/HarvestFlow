# @file secrets/backend.yaml
# @brief 核心配置参数注册表
# @create 2026-03-22
# @note 所有参数均可通过 .env 或命令行参数覆盖
# @note secret 类型由用户标记，level: secret 表示需要加密存储

# ============================================
# 密钥参数（通过 SecretsManager 管理）
# ============================================
secrets: []
# secrets:
#   # --- Infisical SDK ---
#   - name: INFISICAL_CLIENT_ID
#     description: Infisical Client ID (用于 SDK 模式)
#     level: secret
#     default: ""

#   - name: INFISICAL_CLIENT_SECRET
#     description: Infisical Client Secret (用于 SDK 模式)
#     level: secret
#     default: ""

#   - name: INFISICAL_PROJECT_ID
#     description: Infisical Project ID (必填)
#     level: secret
#     default: ""

#   - name: INFISICAL_ENVIRONMENT
#     description: Infisical Environment (dev/staging/prod)
#     level: plain
#     default: "dev"

#   - name: INFISICAL_HOST
#     description: Infisical Host 地址
#     level: plain
#     default: "https://app.infisical.com"

#   - name: INFISICAL_TIMEOUT
#     description: Infisical 连接超时（秒）
#     level: plain
#     default: "10"

#   - name: SECRETS_CACHE_TTL
#     description: 密钥缓存过期时间（秒）
#     level: plain
#     default: "300"
